# Dependencies & build output (rebuilt inside the image)
node_modules
.next
out
build
dist

# Version control & local tooling
.git
.gitignore
.github

# Secrets & local env (provided at runtime via env_file / Railway variables)
.env
.env.*
!.env.example

# Editor / OS noise
.vscode
.idea
.DS_Store
*.log
npm-debug.log*

# Docker & docs not needed in the build context
Dockerfile
.dockerignore
docker-compose.yml
*.md
!prisma/**/*.md

# Test / coverage artifacts
coverage
.next/cache
